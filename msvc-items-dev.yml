server: 
  port: 8002 

config:
  base:
    endpoint:
      msvc-products: http://msvc-products/products

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
    
configuration:
  text: Configuración Entorno Desarrollo
  author:
    name: PruebaJohn
    email: john@mail.com